# CLAUDE.md

## Development

- Use `python3` (not `python`) to run scripts and tests.
- Run tests: `python3 -m pytest tests/`

## Planning

- Prefer the simplest viable fix first. Propose minimal changes before abstractions — only add DRY/SOLID patterns if the simple approach has real downsides.
- Build plans go in the `build-plan/` directory. Existing files: `bugs.md`, `backlog.md`, `agent_tasks.md`, `testing.md`, plus topic-specific plans.
